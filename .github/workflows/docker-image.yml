name: Docker Image CI

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: 'Login to GitHub Container Registry'
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{github.actor}}
        password: ${{secrets.ACCESS_TOKEN}}

    - name: 'Build and push image'
      run: |
        docker build . --tag ghcr.io/kh4st3h/setarify-backend:$(date +%s) --tag ghcr.io/kh4st3h/setarify-backend:latest
        docker push ghcr.io/kh4st3h/setarify-backend:latest

